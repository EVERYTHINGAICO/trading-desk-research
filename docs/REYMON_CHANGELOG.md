# Reymon Changelog

Append-only record for hourly engineering runs. No entry means no verified change.

## 2026-09-09 — BLOCKED — CRITICAL Binance Demo reconciliation
- Evidence: latest exported reconciliation run `187` remains `RUNNING` since `2026-08-29 02:07:29`; run `187` recorded `RECONCILIATION_ERROR` for GASUSDT and prior repeated failures include Binance `-4509`/`-4045` while attempting native protection creation. Pending incident reports current reconciliation discrepancy count `2` and repeated `-4509` eight times in 24h.
- Root cause: reconciliation retries `place_native_protections()` for every historical `PROTECTION_REQUIRED` intent; Binance rejects placement when no live position exists (`-4509`) or stop-order limit is exhausted (`-4045`). Completing repair requires live exchange position/protection evidence and may submit protection orders, forbidden for Reymon.
- Files: `docs/REYMON_CHANGELOG.md` only. Checks: exported SQLite reconciliation runs/issues/errors inspected; changelog trailing-whitespace check passed; `git diff --check` unavailable because repository metadata lacks a valid HEAD. Remaining risk: Demo reconciliation remains unclean; supervising process must obtain current Binance Demo positions plus open algo orders and decide whether to repair or close affected positions.

## 2026-09-11 — BLOCKED — ERROR Binance monitor timestamp skew (incident 3468)
- Evidence: pending incident `3468` reports Binance `-1021` twice in 24 hours, last seen `2026-09-11 17:20:33`; monitor heartbeat returned cleanly at `2026-09-11T19:31:53Z`. Exported database history preserves prior `-1021`: `Binance Demo 400: {"code":-1021,"msg":"Timestamp for this request is outside of the recvWindow."}`.
- Root cause and fix: signed `BinanceDemoClient._request()` used only local time and could not recover from exchange clock skew. Added one bounded retry: fetch public `/fapi/v1/time`, re-sign with Binance `serverTime`, retry original request. No orders, cancels, capital, leverage, risk limits, credentials, `.env`, strategy config, or trading history changed.
- Files: `src/desk/binance_demo.py`, `tests/test_binance_demo.py`, `docs/REYMON_CHANGELOG.md`. Checks: `python3 -m compileall -q src scripts` passed; `git -c safe.directory="$PWD" diff --check` passed. BLOCKED: required focused check `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest -q tests/test_binance_demo.py` could not run because `pytest` is absent and repository has no discovered test environment or dependency manifest. Remaining risk: patch behavior needs focused test execution by supervising process before deployment.

## 2026-09-12 — FIXED — ERROR Binance native protection duplicate (`-4130`)
- Evidence: pending incident `2` reports Binance `-4130` twice in 24 hours, last seen `2026-09-12 10:45:24`. Exported `demo_order_errors` shows `BINANCE_EXECUTION` failures from `PROTECTION_WATCHER`: `An open stop or take profit order with GTE and closePosition in the direction is existing.`
- Root cause and fix: entry executor placed both native SL/TP orders after fill without checking existing same-side protections. Added shared `protection_labels()` and read `open_algo_orders()` before placement; existing same-side `STOP_MARKET`/`TAKE_PROFIT_MARKET` now cover labels and placement skips them. No orders, cancels, capital, leverage, risk limits, credentials, `.env`, strategy config, or trading history changed.
- Files: `src/desk/binance_demo.py`, `scripts/run_binance_demo_once.py`, `scripts/reconcile_binance_demo.py`, `tests/test_binance_demo.py`, `docs/REYMON_CHANGELOG.md`. Checks: direct shared-helper assertion passed; `python3 -m compileall -q src scripts` passed; `git -c safe.directory="$PWD" diff --check` passed. BLOCKED: required focused check `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1 pytest -q tests/test_binance_demo.py` could not run because `pytest` is absent and repository has no discovered test environment or dependency manifest. Remaining risk: supervising process must run focused tests and observe next Demo protection placement; current exchange state was not changed.
